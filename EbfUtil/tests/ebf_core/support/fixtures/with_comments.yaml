# PyYAML should ignore comments
app: myapp
theme: dark   # user override target in tests
paths:
  # nested keys
  data: "/home/user/data"
multiline: |
  First line
  Second line
anchors:
  base: &base
    retries: 5
    timeout_sec: 20
  derived:
    <<: *base
    timeout_sec: 15
